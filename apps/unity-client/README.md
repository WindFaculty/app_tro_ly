# Unity Client App

This Unity project lives under `apps/unity-client` and uses Unity `6000.4.1f1`.

Current implementation: the project now mirrors the repo-side Unity configuration plus the B00/B01 standalone room runtime bundle from `ai-dev-system/clients/unity-client/`.

It is still not wired into the desktop runtime or source-of-truth docs as a replacement for the existing Unity client.
