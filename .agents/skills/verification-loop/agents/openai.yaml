interface:
  display_name: "Verification Loop"
  short_description: "Run build, test, diff, and security checks before closing agent-platform work."
  brand_color: "#10B981"
  default_prompt: "Run verification: tests, surface drift checks, and focused review."
policy:
  allow_implicit_invocation: true
