# Done Log

This file is a historical log.

Rules:

- Entries reflect what was considered complete at the time.
- Current active status belongs in `tasks/task-queue.md` and `tasks/task-people.md`.
- If history later needs clarification, add a note instead of silently rewriting older entries.

## Quick Index

- Docs baseline and tracker cleanup: `D001-D005`, `D009-D011`, `D020-D023`, `D033`, `D040-D042`, `D045-D049`
- Unity shell and UI completion slices: `D006-D008`, `D024-D027`
- Modularization groundwork completed so far: `D033-D036`, `D038-D039`
- Rebuild program baselines: `D043-D044`, `D050-D068`
- Agent-platform refactor and harness adapters: `D052`
- Room vision and art direction freeze: `D062`

## 2026-03-13

- D001: Rebased the active project documentation from Character Studio to the local desktop assistant product.
- D002: Defined the new MVP scope, target architecture, API contract, UI modes, and test plan.
- D003: Reset milestone tracking and task queue around the assistant roadmap.
- D004: Added an architecture-as-is document to describe the transition state.
- D005: Reframed `agent-platform` as optional support rather than the primary runtime.

## 2026-03-14

- D006: Implemented a backend-backed Settings panel in the Unity shell and wired it to the local settings API.
- D007: Updated milestone tracking to reflect that implementation had already moved into polish work.
- D008: Added backend log output, health recovery actions, and Unity degraded-runtime guidance.
- D009: Refreshed AI runtime documentation to match backend orchestration, route selection, and streaming behavior.
- D010: Cleaned task-tracking docs, clarified AI versus manual ownership, and repaired earlier task-doc text issues.

## 2026-03-19

- D011: Rebaselined `tasks/task-people.md` and `tasks/task-queue.md` against verified Unity evidence, manual blockers, and active runtime hardening work.
- D012: Hardened backend TTS degraded-path handling so ChatTTS import or dependency mismatch reports a controlled unavailable state instead of crashing health, TTS, or streaming flows.
- D013: Isolated backend tests from local `.env` overrides so health, scheduler, stream, chat, LLM, and TTS coverage run repeatably in a clean test configuration.
- D014: Added shared Windows preflight helpers, a repeatable backend smoke script, release packaging validation, and reconnect loops for the Unity WebSocket clients.
- D015: Expanded `scripts/smoke_backend.py` to cover REST task mutation flows, event sequencing, speech endpoint availability checks, and assistant-stream completion.
- D016: Added validation-only Piper smoke assets, restored ChatTTS import compatibility for current torch builds, and fixed Piper subprocess Unicode handling so ready-state validation can succeed under controlled overrides.

## 2026-03-20

- D017: Closed `A01` by tightening Windows preflight and release-layout checks and adding automated coverage for those paths.
- D018: Closed `A02` by documenting stable Windows script exit codes, adding `run_all.ps1 -ShutdownBackendOnExit`, and hardening safe cleanup behavior.
- D019: Closed `A06` by completing `docs/09-runbook.md` with setup, startup, smoke, packaging, release-validation, troubleshooting, and evidence-capture guidance.

## 2026-03-26

- D020: Audited root docs, architecture docs, API docs, UI docs, runbook docs, task trackers, and AI guidance files against the current backend, Unity client, scripts, and avatar groundwork.
- D021: Rewrote stale docs to distinguish current implementation, planned work, optional subsystems, placeholders, and manual-validation-needed areas.
- D022: Rebased task trackers so the active queue no longer leaves already-closed work such as `A01`, `A02`, `A03`, and `A06` marked as active.
- D023: Relabeled `unity-client/Assets/Resources/UI/ui_feature_map.md` as a design-target document instead of implementation truth.

## 2026-03-27

- D024: Updated `unity-client/Assets/Tests/PlayMode/AssistantAppPlayModeTests.cs` from legacy UGUI queries to the current UI Toolkit runtime contract and re-verified Unity PlayMode coverage from the active editor session with `25 passed`.
- D025: Landed the first shared UI shell foundation pass by polishing `AppShell.uxml`, adding reusable shell tokens or button or card styles, wiring schedule-side shell insight labels in `AppShellController`, and re-verifying Unity PlayMode coverage from the active editor session with `25 passed`.

## 2026-03-28

- D026: Completed the repo-side `UI-02`, `UI-03`, and `UI-04` pass by rebuilding the Home, Schedule, and Settings UI Toolkit screens around workload cards, quick-add guidance, structured schedule lists, grouped settings summaries, and updated PlayMode coverage; Unity PlayMode was re-verified from the active editor session with `25 passed`.
- D027: Completed manual task `P01` by verifying Unity EditMode tests (21 passed) and PlayMode tests (33 passed).
- D028: Implemented a Windows GUI agent MVP under `ai-dev-system/app/` with a modular controller, pywinauto-first automation, PyAutoGUI fallback wiring, timestamped JSONL and screenshot artifacts, Notepad and Calculator profiles, and a CLI for `list-windows`, `inspect`, and `run`.
- D029: Verified the Windows GUI agent on 2026-03-28 from `ai-dev-system/` with real profile runs for `python -m app.main run --profile calculator --task "compute 125*4"` and `python -m app.main run --profile notepad --task "type hello and save"`, plus automated test evidence from `.\.venv\Scripts\python.exe -m pytest app\tests -q` reporting `2 passed` and `.\.venv\Scripts\python.exe -m pytest tests -q` reporting `15 passed`.
- D030: Implemented the GUI-only `unity-editor` profile under `ai-dev-system/app/` with YAML macro task specs, a `UnityMacroRegistry`, pinned Unity surface and layout mapping, preflight fail-fast checks, per-run `unity-summary.json` artifacts, and unit coverage for macro compilation, preflight, and controller summaries. Verification on 2026-03-28 included `.\.venv\Scripts\python.exe -m pytest tests app\tests\unit -q` reporting `58 passed`, `.\.venv\Scripts\python.exe -m app.main inspect --app unity-editor` completing with control-tree and surface-screenshot artifacts under `ai-dev-system\logs\gui-agent\20260328T171439Z-inspect-unity-editor\`, and `.\.venv\Scripts\python.exe -m app.main --dry-run run --profile unity-editor --task-file %TEMP%\unity-editor-assert-layout.yaml` completing with `unity-summary.json` under `ai-dev-system\logs\gui-agent\20260328T171601Z-unity-editor\`.
- D031: Upgraded `ai-dev-system/app/` on 2026-03-29 from a GUI-only `unity-editor` profile to a hybrid Unity control plane with a per-run Unity MCP runtime, a live capability matrix, MCP-first routing plus GUI fallback for editor-surface tasks, structured `actions[]` and `verify[]` task specs, narrow NL-to-capability parsing, hybrid preflight, and expanded unit coverage. Verification on 2026-03-29 included `.\.venv\Scripts\python.exe -m pytest tests app\tests\unit -q` reporting `66 passed`, `.\.venv\Scripts\python.exe -m app.main list-capabilities --profile unity-editor` returning the live capability matrix from the current Unity session, `.\.venv\Scripts\python.exe -m app.main --dry-run run --profile unity-editor --task-file %TEMP%\unity-hybrid-task.yaml` completing with an MCP-backed dry-run action, and `.\.venv\Scripts\python.exe -m app.main run --profile unity-editor --task-file %TEMP%\unity-hybrid-live-task.yaml` completing a read-only `scene.manage` run with verification against `Assets/Scenes/AIDemoBasic3D.unity`.
- D032: Expanded `ai-dev-system/app/` on 2026-03-29 with platform abstraction contracts, provider-agnostic Vision LLM locator wiring, a bounded `ui_heal` recovery stage, action-level `heal_hints` plus `layout_policy` plus `execution` metadata, background MCP job tracking, MCP-backed `editor.layout.normalize`, surfaced `shader.manage` or `texture.manage` or `vfx.manage` or `animator.graph.manage` capabilities, and explicit `unsupported` statuses for planned graph capabilities such as Timeline and graph-level Shader Graph or VFX Graph mutations. Verification on 2026-03-29 included `.\.venv\Scripts\python.exe -m pytest app\tests\unit -q` reporting `65 passed`, `.\.venv\Scripts\python.exe -m pytest app\tests -q` reporting `67 passed`, `.\.venv\Scripts\python.exe -m pytest tests -q` reporting `15 passed`, and `.\.venv\Scripts\python.exe -m app.main list-capabilities --profile unity-editor` returning the expanded live capability matrix under `ai-dev-system\logs\gui-agent\20260329T161555Z-capabilities-unity-editor\`.

## 2026-04-04

- D033: Completed modularization Phase 0 baseline capture (`A27`) by creating `docs/migration/phase0.md`, refreshing the current-state architecture or UI or README docs, marking `A27` done in `tasks/module-migration-backlog.md`, and updating `tasks/task-queue.md` so later modularization slices stay clearly planned rather than implied current behavior. Evidence preserved the historical backend `62 passed` note from 2026-03-26, the Unity verification notes already tracked on 2026-03-28 and 2026-03-29, and the fresh backend rerun from `local-backend/` on 2026-04-04 with `python -m pytest -q` reporting `69 passed, 1 failed` at `tests/test_tts_service.py::test_chattts_synthesize_writes_cached_wav`.
- D034: Completed shared-core setup (`A28`) inside the current `unity-client/` tree by adding initial module contracts for planner or chat or settings read models, landing a lightweight `AssistantEventBus`, extracting shell-stage snapshot building into shared core, updating `AssistantApp`, `AppShellController`, `HomeScreenController`, and `ScheduleScreenController` to consume the new boundaries, and extending Unity tests with shared-core EditMode coverage plus a refreshed PlayMode shell-controller path. Verification on 2026-04-04 from the active Unity Editor session included a forced refresh plus compile, console recheck with no project-code errors, `run_tests` EditMode for `LocalAssistant.EditModeTests` reporting `25 passed`, and `run_tests` PlayMode for `LocalAssistant.PlayModeTests` reporting `35 passed`. Residual console output remained limited to the known runtime font fallback warnings emitted by `Assets/Scripts/Core/UiDocumentLoader.cs`.
- D035: Completed chat boundary setup (`A29`) inside the current `unity-client/` tree by introducing `IChatModule` plus `ChatModule` as the module-facing chat boundary, moving `AssistantApp` onto that boundary instead of directly owning both `ChatViewModelStore` and `ChatPanelController`, routing text-request creation through `ChatRequestFactory`, and updating the chat panel composition so the route badge is runtime-bound instead of fixed copy. Coverage was refreshed with a new EditMode `ChatModuleTests` slice plus updated chat store or controller or assistant-app tests. Verification on 2026-04-04 from the active Unity Editor session included a forced refresh plus compile, console recheck with no project-code errors, `run_tests` EditMode for `LocalAssistant.EditModeTests` reporting `26 passed`, and `run_tests` PlayMode for `LocalAssistant.PlayModeTests` reporting `35 passed`. Residual console output remained limited to the known runtime font fallback warnings emitted by `Assets/Scripts/Core/UiDocumentLoader.cs`.
- D036: Completed shell module extraction (`A30`) inside the current `unity-client/` tree by introducing `IShellModule` plus `ShellModule` as the shell-facing boundary, keeping `AppShellController` and `AppRouter` as shell internals, moving `AssistantApp` onto the shell-module contract instead of owning those shell details directly, and adding dedicated PlayMode coverage for shell route mounting plus refresh forwarding. Verification on 2026-04-04 from the active Unity Editor session included a forced refresh plus compile, console recheck with no project-code errors, `run_tests` EditMode for `LocalAssistant.EditModeTests` reporting `26 passed`, and `run_tests` PlayMode for `LocalAssistant.PlayModeTests` reporting `39 passed`. Residual console output remained limited to the known runtime font fallback warnings emitted by `Assets/Scripts/Core/UiDocumentLoader.cs`.
- D037: Rewrote the task trackers on 2026-04-04 so `tasks/task-queue.md` now uses four UI workstreams (`UI-1` through `UI-4`) plus a separate cross-cutting section, completed groundwork such as `A28` through `A30` no longer lingers in the active queue, `tasks/task-people.md` now treats `P02` and `P04` as the live manual gates while `P01`, `P03`, and `P05` remain historical evidence, and `tasks/module-migration-backlog.md` now records phased modularization work by lane without duplicating the active queue. Evidence is the synchronized tracker rewrite across `tasks/task-queue.md`, `tasks/task-people.md`, `tasks/module-migration-backlog.md`, and `tasks/p02-manual-checklist.md`.

## 2026-04-05

- D038: Completed planner backend-integration boundary work (`A32`) inside the current `unity-client/` tree by introducing `PlannerBackendIntegration` plus typed planner snapshots or mutation results under `unity-client/Assets/Scripts/Tasks/`, moving `AssistantApp` off ad-hoc planner DTO aggregation for reload or complete or reschedule flows, updating `TaskViewModelStore` plus Home or Schedule consumers to read typed planner models instead of raw transport payloads, and adding EditMode coverage for the transformed planner data path. Verification on 2026-04-05 from the active Unity Editor session included a forced refresh plus compile, console recheck with no project-code errors, `run_tests` EditMode for `LocalAssistant.EditModeTests` reporting `28 passed`, and `run_tests` PlayMode for `LocalAssistant.PlayModeTests` reporting `41 passed`. Residual runtime output remained limited to the known UI Toolkit font fallback warnings and repeated multiple-audio-listener warnings emitted during existing PlayMode tests, not new project-code failures.
- D039: Completed chat isolation and enhancement work (`A33`) inside the current `unity-client/` tree by extending `IChatModule` plus `ChatModule` to own turn-start reduction, compatibility reply handling, assistant-stream transcript or route or chunk or final handling, and planner-action summaries; moving `AssistantApp` onto those chat-owned APIs instead of assembling transcript or diagnostics or planner confirmation state inline; updating `ChatViewModelStore` to unify compatibility and streaming render state; and adding focused EditMode plus PlayMode coverage for the unified chat path. Verification on 2026-04-05 from the active Unity Editor session included a forced refresh plus compile, console recheck with zero project-code errors, `run_tests` EditMode for `LocalAssistant.EditModeTests` reporting `30 passed`, and `run_tests` PlayMode for `LocalAssistant.PlayModeTests` reporting `42 passed`. Residual runtime output remained limited to the known UI Toolkit font fallback warnings emitted by `Assets/Scripts/Core/UiDocumentLoader.cs`.

## 2026-04-07

- D040: Completed the `ai-dev-system` root-unification Phase 0 freeze by auditing the root directories outside `local-backend/`, classifying each migration candidate as `absorb into ai-dev-system`, `wrap by temporary adapter`, `archive/workbench`, or `delete after migrate`, and adding `docs/migration/ai-dev-system-unification-phase0.md` as the baseline record. The same pass refreshed `README.md`, `docs/index.md`, `docs/roadmap.md`, `docs/migration/phase0.md`, and `tasks/task-queue.md` so the new baseline is visible without mislabeling the current runtime layout. Evidence for this history entry is the 2026-04-07 root-directory inventory, the reviewed `ai-dev-system/README.md`, and the path-reference audit showing that `unity-client/` plus root `scripts/` are still current paths today.
- D041: Completed the `ai-dev-system` unification Phase 1 architecture pass by rewriting `ai-dev-system/README.md` to define the subsystem as the non-backend integration root, adding `ai-dev-system/AGENTS.md`, creating the target layer scaffolding under `ai-dev-system/control-plane/`, `clients/`, `domain/`, `context/`, `asset-pipeline/`, `workbench/`, and `scripts/`, and recording the result in `docs/migration/ai-dev-system-unification-phase1.md`. The same pass shortened root `AGENTS.md` back to repo-level governance and refreshed root docs plus task tracking so the new layer directories are documented as architecture ownership rather than claimed path moves. Evidence for this history entry is the post-edit `ai-dev-system/` directory inventory, the reviewed subsystem README or AGENTS files, and the cross-reference audit covering `README.md`, `docs/index.md`, `docs/roadmap.md`, `docs/migration/phase0.md`, and `tasks/task-queue.md`.
- D043: Completed rebuild Phase 0 — Freeze hiện trạng và khóa target-state. Created `docs/migration/rebuild-target.md` capturing current repo state (unity-client shell UI, local-backend services, API endpoints), declaring the architecture target (Tauri + React/Vite + Unity 3D runtime + FastAPI), listing Unity parts to keep (AvatarStateMachine, LipSyncController, AvatarSystem, room scene, camera, animation) and to retire (shell layout, Chat/Schedule/Settings/Home UI features, all *Refs.cs UI controllers, UXML/USS shell templates). Created `docs/migration/rebuild-rules.md` with 8 locked migration rules (Unity no business UI, React owns business UI, backend is truth source, Tauri is sole process host, Unity is embedded 3D runtime, no ad-hoc Unity calls from React, typed contracts mandatory, no new features in old Unity shell UI). Created `tasks/rebuild-master-plan.md` with all 11 phases documented and Phase 0 marked DONE. Updated `tasks/task-queue.md` with the Rebuild Workstream (RB-00 through RB-11).
- D044: Completed rebuild Phase 1 — Thiết kế boundary mới. Created `docs/architecture/ownership-matrix.md` with full Unity feature inventory (55 script files + 14 UXML/USS files, verified from actual codebase 2026-04-07), tagging every feature as keep/retire/partial-keep. Inventoried all 17 backend REST+WebSocket endpoints confirmed reusable by React without changes. Defined Unity bridge event list (10 React→Unity commands, 6 Unity→React events). Drew 4-layer boundary diagram (Tauri/React/Unity/Backend). Created `docs/migration/phase1-boundary.md` with acceptance summary. Updated task-queue.md marking RB-01 DONE and unblocking RB-02.
- D042: Completed the `ai-dev-system` unification Phase 2 context migration by absorbing the root `ai/` files into `ai-dev-system/context/`, folding the old `ai-dev-system/prompts/` content into `ai-dev-system/context/prompts/automation/`, updating subsystem and repo guidance to point at the new context root, and recording the move in `docs/migration/ai-dev-system-unification-phase2.md`. Evidence for this history entry is the pre-move readout of the old `ai/` and `ai-dev-system/prompts/` files, the post-edit inventory of `ai-dev-system/context/`, and the reference audit showing current docs now point to `ai-dev-system/context/` rather than root `ai/` for subsystem context.
- D045: Completed the `ai-dev-system` unification Phase 4 control-plane move by relocating the automation runtime roots into `ai-dev-system/control-plane/`, moving the shared MCP client to `ai-dev-system/control-plane/mcp_client.py`, adding root-level compatibility shims for the old import paths, updating workflow entry scripts and tests to resolve the new layout, and refreshing repo guidance so `control-plane/` is documented as current implementation rather than planned-only scaffolding. Evidence for this history entry is the post-move `ai-dev-system/` inventory showing the runtime now under `control-plane/`, the reviewed `ai-dev-system/control-plane/README.md` plus updated repo docs, and the recorded verification attempt from `ai-dev-system/` showing `python -m pytest tests control-plane/app/tests -q` was blocked because this shell has no usable Python interpreter on PATH and no repo-local `.venv/`.
- D046: Completed the `ai-dev-system` unification Phase 3 client move by relocating the Unity project from repo root `unity-client/` into `ai-dev-system/clients/unity-client/`, updating current-state docs and trackers to use the absorbed client path, and adding script or control-plane path resolution so repo entry points no longer assume the old root layout. Evidence for this history entry is the post-move inventory showing root `unity-client/` is gone while `ai-dev-system/clients/unity-client/` now contains `Assets/`, `Packages/`, and `ProjectSettings/`, plus the path-reference audit covering `README.md`, `AGENTS.md`, `docs/index.md`, `docs/roadmap.md`, `tasks/task-queue.md`, and `tasks/task-people.md`. Manual Unity Editor and packaged-client smoke remain tracked under `P02`.
- D047: Completed the `ai-dev-system` unification Phase 7 scripts-and-tests pass by turning `ai-dev-system/scripts/` into the standardized non-backend command surface, adding Phase 7 drift validation under `ai-dev-system/scripts/validate/` plus `ai-dev-system/tests/structure/`, documenting the new ownership in `ai-dev-system/README.md`, `ai-dev-system/AGENTS.md`, and `docs/migration/ai-dev-system-unification-phase7.md`, and refreshing `tasks/task-queue.md` so the new entry-point and validation roots are treated as current implementation. Evidence for this history entry is the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-structure.ps1`, the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-avatar-pipeline.ps1`, the successful stdlib-only run of `python ai-dev-system/scripts/validate/validate_phase7_structure.py`, and the successful repo-side coverage run of `python -m unittest discover -s ai-dev-system/tests/structure -p "test_*.py"`.
- D048: Completed the `ai-dev-system` unification Phase 8 docs-and-task-governance rewrite by replacing the old multi-root framing in `docs/roadmap.md` with a current non-backend integration view centered on `ai-dev-system/`, adding `docs/architecture/non-backend-integration.md`, adding the overview page `docs/migration/ai-dev-system-unification.md`, recording the phase in `docs/migration/ai-dev-system-unification-phase8.md`, creating `tasks/ai-dev-system-unification-backlog.md` with the new lanes (`Control Plane`, `Unity Client`, `Avatar + Customization`, `Asset Pipeline`, `Governance + Validation`), and updating `tasks/task-queue.md` plus `tasks/module-migration-backlog.md` so the new tracker is explicit without erasing older modularization history. Evidence for this history entry is the successful terminal run of `python ai-dev-system/scripts/validate/validate_unification_docs_tasks.py`, the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-docs-tasks.ps1`, and the successful repo-side coverage run of `python -m unittest discover -s ai-dev-system/tests/structure -p "test_*.py"` after the new docs-and-tasks drift check landed.
- D049: Completed the `ai-dev-system` unification Phase 9 architecture-lock pass by removing the temporary root-level control-plane shim packages from `ai-dev-system/`, adding the supported bootstrap surface in `bootstrap_control_plane.py` plus `sitecustomize.py` plus the standardized run wrappers, retiring the stale `unity-client/` fallback from `scripts/assistant_common.ps1`, updating active docs and task trackers to stop describing removed shim paths as current behavior, and adding Phase 9 drift validation in `ai-dev-system/scripts/validate/validate_phase9_architecture_lock.py`, `ai-dev-system/scripts/validate/validate-architecture-lock.ps1`, and `ai-dev-system/tests/structure/test_phase9_architecture_lock.py`. Evidence for this history entry is the successful terminal run of `python ai-dev-system/scripts/validate/validate_phase9_architecture_lock.py`, the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-architecture-lock.ps1`, the successful repo-side coverage run of `python -m unittest discover -s ai-dev-system/tests/structure -p "test_*.py"`, the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-structure.ps1`, and the Phase 9 wrapper checks showing `ai-dev-system/scripts/run/run-gui-agent.ps1 --help` plus `ai-dev-system/scripts/run/inspect-unity-profile.ps1 --help` now fail on the missing external `mcp` dependency rather than removed root shim imports.
- D050: Completed phase `A00` by creating `docs/product/desktop-product-definition.md`, `docs/product/desktop-scope-v1.md`, and `docs/product/desktop-non-goals.md` to freeze the standalone desktop app scope, non-goals, and per-module acceptance criteria under the locked architecture `Tauri Desktop Host + React/Vite Web UI + Unity Runtime + FastAPI Backend`. The same pass refreshed `docs/roadmap.md` so the new A/B/S rebuild program is visible as planned work without relabeling the current Unity-first runtime as shipped behavior, rewrote `tasks/task-queue.md` to use the active `Axx`, `Bxx`, and `Sxx` execution model with explicit gates that keep Workstream A and Workstream B independent until the sync series, and relabeled `tasks/rebuild-master-plan.md` as historical planning context. Evidence for this history entry is the post-edit readback of the new product docs, the updated roadmap links, the rebuild-plan status note, and the synchronized queue reset showing `A00` as `DONE`, `A01` as the next desktop phase, B-series held behind the shared execution-surface gate, and S-series blocked until `A15` plus `B11`.
- D051: Completed phase `A01` by creating `docs/architecture/desktop-target.md`, `docs/architecture/desktop-runtime-flow.md`, `docs/architecture/local-storage.md`, plus ADRs under `docs/adr/` to lock desktop ownership boundaries, runtime flow, config ownership, and the target SQLite versus JSON persistence split. The same pass refreshed `docs/roadmap.md` so the architecture docs sit in the active read path, and updated `tasks/task-queue.md` so `A01` is `DONE` and `A02` is the next repo phase. Evidence for this history entry is the post-edit readback of the new architecture docs and ADRs, the code-backed references to `apps/desktop-shell/src-tauri/src/lib.rs`, `apps/web-ui/src/services/runtimeHost.ts`, `packages/contracts/src/tauri.ts`, `packages/contracts/src/unity.ts`, `local-backend/app/core/config.py`, and `local-backend/app/db/repository.py`, plus the synchronized task tracker update.
- D052: Refactored the non-backend agent platform around a canonical control-plane core by adding `ai-dev-system/control-plane/catalog/platform.json` for agent, skill, workflow, rule-pack, eval, and tool-policy specs; routing `ai-dev-system/workflows/autonomous_loop.py` through the new lifecycle engine in `ai-dev-system/control-plane/orchestrator/`; adding generated Codex and Antigravity surfaces through `ai-dev-system/control-plane/adapters/` plus `ai-dev-system/scripts/sync-agent-surfaces.py`; and documenting the new reality in `docs/architecture/agent-platform-baseline.md`, `docs/architecture/ecc-inspired-gap-analysis.md`, and `docs/architecture/target-agent-platform-architecture.md`. Evidence for this history entry is the successful terminal run of `python -m unittest discover -s ai-dev-system/tests -p "test_*.py"`, the successful terminal run of `python ai-dev-system/scripts/validate/validate_agent_platform_surfaces.py`, the successful terminal run of `python ai-dev-system/scripts/sync-agent-surfaces.py --check`, and the generated harness surfaces now present under `.codex/`, `.agents/skills/`, and `.agent/`.
- D053: Completed the current rebuild phase `A02` by adding root `package.json` workspaces plus repo-level `web:*`, `desktop:*`, and `rebuild:check` scripts for `apps/web-ui/`, `apps/desktop-shell/`, and `packages/contracts/`; adding explicit `check` scripts to `apps/web-ui/package.json` and `apps/desktop-shell/package.json`; aligning the default browser-preview backend URL in `apps/web-ui/src/services/runtimeHost.ts` with the Tauri host default in `apps/desktop-shell/src-tauri/src/backend.rs` at `http://127.0.0.1:8096`; and adding `scripts/validate_desktop_execution_surface.py` to guard workspace-hook and backend-URL drift. The same pass refreshed `docs/architecture/desktop-target.md`, `docs/architecture/desktop-runtime-flow.md`, `docs/roadmap.md`, and `tasks/task-queue.md` so the active docs and queue match the new execution surface, and it unblocked `B00` in the active queue. Evidence for this history entry is the successful terminal run of `python scripts/validate_desktop_execution_surface.py`, the successful terminal run of `npm run web:build`, the successful terminal run of `cargo check` from `apps/desktop-shell/src-tauri/`, and the successful repo-root run of `npm run rebuild:check`.
- D054: Completed the current rebuild phase `A03` by extending `apps/desktop-shell/src-tauri/src/backend.rs` plus `apps/desktop-shell/src-tauri/src/lib.rs` so the Tauri host now owns backend process state, startup status plus ready plus error events, retry entry points, runtime-path snapshots, window-control commands, and shutdown cleanup for backend plus Unity sidecars; extending `packages/contracts/src/tauri.ts` plus `apps/web-ui/src/services/runtimeHost.ts` so React consumes those host surfaces through typed contracts; adding desktop startup recovery and custom window chrome in `apps/web-ui/src/components/StartupScreen.tsx` plus `apps/web-ui/src/components/WindowChrome.tsx`; and updating `apps/web-ui/src/components/ShellLayout.tsx` plus `apps/web-ui/src/pages/StatusPage.tsx` to surface host state inside the desktop shell. The same pass refreshed `docs/architecture/desktop-target.md`, `docs/architecture/desktop-runtime-flow.md`, `docs/roadmap.md`, and `tasks/task-queue.md` so the current implementation notes match the landed host behavior. Evidence for this history entry is the successful terminal run of `npm run web:build`, the successful terminal run of `cargo check` from `apps/desktop-shell/src-tauri/`, and the successful repo-root run of `npm run rebuild:check`.
- D055: Completed the current rebuild phase `A04` by adding `apps/desktop-shell/src-tauri/src/persistence.rs` plus new restore and persist commands in `apps/desktop-shell/src-tauri/src/lib.rs` so the Tauri host now owns versioned JSON restore files for session state, window state, runtime snapshots, theme state, filters, and app preferences under the app data root, including corrupted-file quarantine and window-state capture or restore; extending `packages/contracts/src/tauri.ts` plus `apps/web-ui/src/services/runtimeHost.ts` so React can load and save those typed desktop restore surfaces while browser preview falls back to `localStorage`; and updating `apps/web-ui/src/App.tsx`, `apps/web-ui/src/components/ShellLayout.tsx`, and `apps/web-ui/src/pages/StatusPage.tsx` so the shell restores the last route, auto-saves desktop session state, and exposes persistence diagnostics inside the status view. The same pass refreshed `docs/architecture/desktop-target.md`, `docs/architecture/desktop-runtime-flow.md`, `docs/architecture/local-storage.md`, `docs/roadmap.md`, and `tasks/task-queue.md` so the queue and architecture notes match the landed persistence layer. Evidence for this history entry is the successful terminal run of `cargo test --manifest-path src-tauri/Cargo.toml` from `apps/desktop-shell/`, the successful terminal run of `cargo check --manifest-path src-tauri/Cargo.toml` from `apps/desktop-shell/`, the successful terminal run of `npm run web:build`, the successful terminal run of `python scripts/validate_desktop_execution_surface.py`, and the successful repo-root run of `npm run rebuild:check`.
- D056: Completed the current rebuild phase `A05` by rebuilding `apps/web-ui/src/styles/globals.css` into a shared desktop design system with dedicated typography, tokens, surfaces, buttons, metric cards, and utility primitives; expanding `apps/web-ui/src/components/PageTemplate.tsx` plus `apps/web-ui/src/components/PageTemplate.module.css` into the shared module-shell frame for page heroes and highlight strips; refreshing `apps/web-ui/src/components/ShellLayout.tsx` plus `apps/web-ui/src/components/ShellLayout.module.css` and `apps/web-ui/src/components/WindowChrome.module.css` so the desktop shell now has a landed navigation rail, center-stage composition, and aligned chrome instead of the earlier scaffold; and restyling `apps/web-ui/src/pages/DashboardPage.tsx`, `ChatPage.tsx`, `PlannerPage.tsx`, `WardrobePage.tsx`, `SettingsPage.tsx`, and `StatusPage.tsx` so the existing backend-backed pages all consume the same design language. The same pass refreshed `docs/architecture/desktop-target.md`, `docs/architecture/desktop-runtime-flow.md`, `docs/roadmap.md`, and `tasks/task-queue.md` so the current implementation notes and queue match the landed shell UI. Evidence for this history entry is the successful terminal run of `npm run web:build`, the successful terminal run of `cargo check --manifest-path src-tauri/Cargo.toml` from `apps/desktop-shell/`, and the successful repo-root run of `npm run rebuild:check`.
- D057: Completed the current rebuild phase `A06` by replacing the A05 placeholder chat page with a real assistant workspace in `apps/web-ui/src/pages/ChatPage.tsx`, `apps/web-ui/src/pages/ChatPage.module.css`, `apps/web-ui/src/features/chat/useAssistantWorkspace.ts`, and `apps/web-ui/src/services/assistantStream.ts`, so the desktop shell now supports backend-backed conversation threads, stream-first transcript state, retry, local search, routing diagnostics, structured action cards, and REST fallback when the assistant stream cannot start. The same pass extended `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts` for typed chat-history and stream surfaces, added read-only history routes plus stored assistant-turn metadata in `local-backend/app/api/routes.py`, `local-backend/app/services/conversation.py`, `local-backend/app/db/repository.py`, `local-backend/app/models/schemas.py`, and `local-backend/app/services/assistant_orchestrator.py`, and refreshed `docs/03-api.md` plus `tasks/task-queue.md` so the current backend contract and active queue match the landed implementation. Evidence for this history entry is the successful terminal run of `npm run web:build`, the successful repo-root run of `npm run rebuild:check`, and the successful inline FastAPI `TestClient` verification from `local-backend/.venv/Scripts/python.exe` covering `POST /v1/chat`, `GET /v1/chat/conversations`, and `GET /v1/chat/conversations/{conversation_id}`.
- D058: Completed the current rebuild phase `A07` by replacing the A05 placeholder planner page with a backend-backed planner workspace in `apps/web-ui/src/pages/PlannerPage.tsx`, `apps/web-ui/src/pages/PlannerPage.module.css`, `apps/web-ui/src/features/planner/usePlannerWorkspace.ts`, `apps/web-ui/src/features/shell/DesktopSessionContext.tsx`, and `apps/web-ui/src/components/ShellLayout.tsx`, so the desktop shell now supports task create or edit or complete flows, today or week or inbox or overdue or completed views, derived reminders and tags views, a calendar lens, conflict summaries, and persisted planner-view restore. The same pass extended `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts` and `packages/contracts/src/tauri.ts` plus `apps/web-ui/src/services/runtimeHost.ts` plus `apps/desktop-shell/src-tauri/src/persistence.rs` so planner read models, task mutations, and planner-view persistence are typed end-to-end, and hardened `local-backend/app/core/time.py` plus `local-backend/tests/test_tasks_api.py` so timezone-aware task due dates no longer crash `/v1/tasks/today` or `/v1/tasks/overdue`. Evidence for this history entry is the successful terminal run of `npm run web:build`, the successful repo-root run of `npm run rebuild:check`, and the successful inline FastAPI `TestClient` verification from `local-backend/.venv/Scripts/python.exe` covering task create or update or complete or reschedule plus `GET /v1/tasks/today`, `GET /v1/tasks/week`, `GET /v1/tasks/overdue`, `GET /v1/tasks/inbox`, and `GET /v1/tasks/completed`.
- D059: Completed the current rebuild phase `A08` by adding notes persistence plus note CRUD and personal-knowledge backend surfaces in `local-backend/app/services/notes.py`, `local-backend/app/api/routes.py`, `local-backend/app/models/schemas.py`, `local-backend/app/db/repository.py`, `local-backend/app/container.py`, `local-backend/app/services/memory.py`, and `local-backend/app/services/tasks.py`, so the backend now exposes `/v1/notes`, `/v1/memory/items`, and `/v1/tasks/active` with SQLite-backed note records, link validation against tasks or chat conversations, and read-only memory retrieval. The same pass extended `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts`, `apps/web-ui/src/App.tsx`, and `apps/web-ui/src/components/ShellLayout.tsx`, then built the real notes module in `apps/web-ui/src/features/notes/useKnowledgeWorkspace.ts`, `apps/web-ui/src/pages/NotesPage.tsx`, and `apps/web-ui/src/pages/NotesPage.module.css` with quick capture, richer note detail, task or chat linking, local cross-module search, and personal knowledge cards backed by the current memory store. The docs sync refreshed `docs/03-api.md` plus `docs/architecture/local-storage.md` so the current backend contract and SQLite scope stay aligned with the landed implementation. Evidence for this history entry is the successful terminal run of `npm run web:check`, the successful terminal run of `npm run web:build`, the successful repo-root run of `npm run rebuild:check`, the successful inline FastAPI `TestClient` verification from `local-backend/.venv/Scripts/python.exe` covering notes plus memory plus active tasks, and the successful `py_compile` verification for the new backend and test files under `local-backend/`.
- D060: Completed the current rebuild phase `A14` by replacing the A05 read-only `SettingsPage` placeholder with a real settings and recovery workspace in `apps/web-ui/src/pages/SettingsPage.tsx` plus `apps/web-ui/src/pages/SettingsPage.module.css`, so the desktop shell now supports backend-backed voice or reminder or memory setting edits, persisted local shell preferences for theme plus planner-view plus restore behavior, privacy-oriented data-path visibility, integrated diagnostics, and safe recovery actions from one page. The same pass extended `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts`, `packages/contracts/src/tauri.ts`, and `apps/web-ui/src/services/runtimeHost.ts` for typed settings reset plus desktop restore reset surfaces; added backend reset support in `local-backend/app/db/repository.py`, `local-backend/app/services/settings.py`, `local-backend/app/api/routes.py`, and `local-backend/tests/test_scheduler_and_health.py`; added desktop restore reset support in `apps/desktop-shell/src-tauri/src/lib.rs` plus `apps/desktop-shell/src-tauri/src/persistence.rs`; and updated `apps/web-ui/src/components/ShellLayout.tsx`, `apps/web-ui/src/pages/StatusPage.tsx`, and `apps/web-ui/src/styles/globals.css` so theme preferences now apply live and the host-diagnostics preference affects the diagnostics lane. The docs sync refreshed `docs/03-api.md`, and the active queue was updated in `tasks/task-queue.md`. Evidence for this history entry is the successful terminal run of `npm run web:check`, the successful terminal run of `npm run web:build`, the successful terminal run of `cargo test` from `apps/desktop-shell/src-tauri/`, the successful terminal run of `cargo check` from `apps/desktop-shell/src-tauri/`, the successful terminal run of `py -3 scripts/validate_desktop_execution_surface.py`, and the successful inline reset verification from `local-backend/.venv/Scripts/python.exe` covering the new settings-reset service behavior.
- D062: Completed `B00` by freezing the room vision and art direction for Workstream B with three new documents: `docs/product/room-vision.md` locking the cozy Japanese-style apartment room concept, four-zone spatial layout (overview, avatar, desk, wardrobe), warm anime-inspired color palette, emotional tone, scope boundaries, and React page-to-room mapping; `docs/product/room-art-direction.md` locking URP Forward rendering, semi-stylized anime art style, orthographic camera with smooth transitions, warm daylight primary lighting, soft post-processing stack, performance budgets (40K-80K triangles, under 100 draw calls, stable 60 FPS target), and material plus texture naming conventions; and `docs/product/room-asset-spec.md` defining the prop inventory with 6 required blockout props and 16 production props, prop interaction levels (static, focusable, animated, interactive), Blender import checklist, Unity RoomSystem folder layout, and 5 scene test requirements. The same pass updated `ai-dev-system/domain/room/README.md` to reference the new documents and updated `tasks/task-queue.md` so `B00` is `DONE` and `B01` is now `TODO`.
- D061: Completed the Mesh AI -> Blender -> validation -> Unity handoff foundation phase under `A17` by adding `docs/architecture/mesh-ai-blender-unity-integration.md`, landing Mesh AI lifecycle profiles plus schemas plus manifests and tool mapping under `ai-dev-system/asset-pipeline/`, adding Mesh AI intake governance under `ai-dev-system/workbench/`, adding deterministic wrapper planning plus planner/executor/workflow foundations under `ai-dev-system/control-plane/` and `ai-dev-system/workflows/`, adding Mesh AI validation entry points under `ai-dev-system/scripts/validate/` and `ai-dev-system/tests/`, and refreshing the active README or roadmap or AGENTS or domain or task-tracking docs so current implementation versus planned work stays explicit. Evidence for this history entry is the successful terminal run of `python -m unittest discover -s ai-dev-system/tests -p "test_*.py"`, the successful terminal run of `python ai-dev-system/scripts/validate/validate_mesh_ai_pipeline.py`, the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-mesh-ai-pipeline.ps1`, the successful terminal run of `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/validate/validate-structure.ps1`, and the successful terminal run of `python ai-dev-system/workflows/mesh_ai_refine.py --intake-manifest ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-intake.json --output-dir %TEMP%\mesh-ai-workflow-check`.
- D063: Completed the current rebuild phase `A09` by adding backend-owned Gmail integration surfaces in `local-backend/app/services/google_email.py` plus `local-backend/app/api/routes.py` for Google OAuth connect or callback or disconnect, inbox search, message detail, local draft CRUD, Gmail send, and email-to-task conversion; extending `local-backend/app/db/repository.py`, `local-backend/app/models/schemas.py`, `local-backend/app/services/settings.py`, and `local-backend/app/container.py` so SQLite now persists Google OAuth token state plus local email drafts and email-task links, and the settings model now carries Gmail sync defaults; extending `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts` and adding `apps/web-ui/src/features/email/useEmailWorkspace.ts`, `apps/web-ui/src/pages/EmailPage.tsx`, and `apps/web-ui/src/pages/EmailPage.module.css` so the React desktop shell now exposes a real Gmail workspace for connect-state visibility, inbox search, reply drafting, send, and task conversion; and updating `apps/web-ui/src/App.tsx`, `apps/web-ui/src/components/ShellLayout.tsx`, and `apps/web-ui/src/pages/SettingsPage.tsx` so the shell navigation and settings center surface Google email auth plus sync state. The docs sync refreshed `docs/03-api.md`, `docs/architecture/local-storage.md`, `tasks/task-queue.md`, and `tasks/task-people.md` so current implementation and the remaining live-credential manual prerequisite stay explicit. Evidence for this history entry is the successful terminal run of `local-backend/.venv/Scripts/python.exe -m pytest local-backend/tests/test_email_api.py local-backend/tests/test_scheduler_and_health.py -q`, the successful terminal run of `npm run web:check`, and the successful terminal run of `npm run web:build`.
- D064: Completed the current rebuild phase `A10` by adding backend-owned Google Calendar integration surfaces in `local-backend/app/services/google_calendar.py` plus `local-backend/app/api/routes.py` for Google OAuth connect or callback or disconnect, agenda status, agenda reads, and provider-backed event create or update or delete; extending `local-backend/app/models/schemas.py`, `local-backend/app/services/settings.py`, `local-backend/app/core/config.py`, and `local-backend/app/container.py` so Google Calendar contracts, settings defaults, redirect handling, and service wiring are typed end to end; extending `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts` and adding `apps/web-ui/src/features/calendar/useGoogleCalendarWorkspace.ts` so the React desktop shell now exposes a typed Google Calendar agenda and mutation workspace; and updating `apps/web-ui/src/pages/PlannerPage.tsx`, `apps/web-ui/src/pages/PlannerPage.module.css`, and `apps/web-ui/src/pages/SettingsPage.tsx` so the planner calendar lens now shows provider-backed agenda events with local event editing while the settings center surfaces Google Calendar auth plus sync defaults. The docs sync refreshed `docs/03-api.md`, `docs/architecture/local-storage.md`, `tasks/task-queue.md`, and `tasks/task-people.md` so current implementation and the remaining live-credential manual prerequisite stay explicit. Evidence for this history entry is the successful terminal run of `local-backend/.venv/Scripts/python.exe -m pytest local-backend/tests/test_calendar_api.py local-backend/tests/test_email_api.py local-backend/tests/test_scheduler_and_health.py -q`, the successful terminal run of `npm run web:check`, and the successful terminal run of `npm run web:build`. Manual validation required: live Google OAuth plus Google Calendar smoke still depend on `P06`.
- D065: Completed the current rebuild phase `A11` by adding approval-first browser automation contracts plus persistence in `local-backend/app/models/schemas.py`, `local-backend/app/db/repository.py`, `local-backend/app/services/browser_automation.py`, `local-backend/app/api/routes.py`, and `local-backend/app/container.py`, so the backend now exposes bounded automation templates, run creation, per-step approve or reject or cancel flows, deterministic `open_url` plus `fetch_page` plus `manual_checkpoint` handlers, and SQLite-backed run plus step plus audit-log history with recovery notes. The same pass extended `apps/web-ui/src/contracts/backend.ts`, `apps/web-ui/src/services/backendClient.ts`, and `packages/contracts/src/unity.ts`, then added `apps/web-ui/src/features/browser-automation/useBrowserAutomationWorkspace.ts`, `apps/web-ui/src/pages/AutomationPage.tsx`, and `apps/web-ui/src/pages/AutomationPage.module.css`, and updated `apps/web-ui/src/App.tsx` plus `apps/web-ui/src/components/ShellLayout.tsx` so the React desktop shell now includes a dedicated browser automation module with a run builder, step-by-step approval lane, audit trail, and route-aware shell navigation. The docs sync refreshed `docs/03-api.md`, `docs/architecture/local-storage.md`, and `tasks/task-queue.md` so current implementation and storage truth stay explicit. Evidence for this history entry is the successful terminal run of `local-backend/.venv/Scripts/python.exe -m pytest local-backend/tests/test_browser_automation_api.py local-backend/tests/test_email_api.py local-backend/tests/test_notes_api.py local-backend/tests/test_scheduler_and_health.py -q`, the successful terminal run of `npm run web:check`, the successful terminal run of `npm run web:build`, and the successful repo-root terminal run of `npm run rebuild:check`.
- D066: Completed the current rebuild phase `A12` by extending `apps/web-ui/src/features/chat/useAssistantWorkspace.ts`, `apps/web-ui/src/features/chat/pushToTalkRecorder.ts`, `apps/web-ui/src/pages/ChatPage.tsx`, and `apps/web-ui/src/pages/ChatPage.module.css` so the React desktop shell now supports push-to-talk voice turns with hold-to-talk WAV capture, stream-first `voice_chunk` plus `voice_end` transport, transcript preview, STT plus chat REST fallback, and queued backend TTS reply playback in the existing conversation workspace. The same pass extended `apps/web-ui/src/contracts/backend.ts`, `apps/web-ui/src/services/backendClient.ts`, and `apps/web-ui/src/pages/SettingsPage.tsx` so speech STT responses, backend audio asset URLs, transcript-preview preferences, and the locked push-to-talk voice mode are typed and surfaced end to end; normalized backend voice settings to `push_to_talk` in `local-backend/app/services/settings.py`; and hardened `local-backend/app/db/repository.py` plus `local-backend/tests/test_scheduler_and_health.py` so settings regressions and chat conversation summaries stay deterministic under the new verification pass. The docs sync refreshed `docs/03-api.md` plus `tasks/task-queue.md` so current implementation notes and the active queue match the landed voice behavior. Evidence for this history entry is the successful terminal run of `local-backend/.venv/Scripts/python.exe -m pytest local-backend/tests/test_chat_api.py local-backend/tests/test_assistant_stream_api.py local-backend/tests/test_scheduler_and_health.py -q`, the successful terminal run of `npm run web:check`, the successful terminal run of `npm run web:build`, and the successful repo-root terminal run of `npm run rebuild:check`. Manual validation required: target-machine microphone capture and speaker playback quality were not exercised from terminal-only repo access.
- D067: Completed the current rebuild phase `A13` by adding the JSON-backed wardrobe registry service in `local-backend/app/services/wardrobe.py` plus new `/v1/wardrobe`, `/v1/wardrobe/export`, `/v1/wardrobe/import`, item CRUD, and preset CRUD routes in `local-backend/app/api/routes.py`, `local-backend/app/models/schemas.py`, and `local-backend/app/container.py`, so the backend now owns canonical slot-taxonomy seeding from `ai-dev-system/domain/customization/`, wardrobe item or preset persistence under `local-backend/data/wardrobe/registry.json`, compatibility validation, and sync-ready import or export flows without claiming live Unity runtime mapping. The same pass extended `apps/web-ui/src/contracts/backend.ts` plus `apps/web-ui/src/services/backendClient.ts`, then added `apps/web-ui/src/features/wardrobe/useWardrobeWorkspace.ts`, rewrote `apps/web-ui/src/pages/WardrobePage.tsx`, and added `apps/web-ui/src/pages/WardrobePage.module.css` so the React desktop shell now has a real wardrobe workspace for slot taxonomy review, registry editing, preset authoring, compatibility inspection, and JSON import or export. The docs sync refreshed `docs/03-api.md`, `docs/architecture/local-storage.md`, `ai-dev-system/domain/customization/README.md`, and `tasks/task-queue.md` so the current contract and storage truth stay aligned with the landed implementation. Evidence for this history entry is the successful terminal run of `local-backend/.venv/Scripts/python.exe -m pytest local-backend/tests/test_wardrobe_api.py local-backend/tests/test_notes_api.py local-backend/tests/test_scheduler_and_health.py -q`, the successful terminal run of `npm run web:check`, the successful terminal run of `npm run web:build`, and the successful repo-root terminal run of `npm run rebuild:check`.
- D068: Completed the current rebuild phase `A15` by adding bundle staging plus package validation in `scripts/prepare_desktop_bundle_resources.py`, `scripts/validate_desktop_bundle.py`, `apps/desktop-shell/package.json`, and `scripts/validate_desktop_execution_surface.py`, so Tauri builds now stage bundle-safe `local-backend/` runtime assets plus wardrobe customization contracts without live backend data or tests and validate the package surface before cargo checks. The same pass hardened `apps/desktop-shell/src-tauri/tauri.conf.json`, `apps/desktop-shell/src-tauri/capabilities/default.json`, `apps/desktop-shell/src-tauri/Cargo.toml`, `apps/web-ui/src/styles/globals.css`, and `apps/web-ui/index.html` so the packaged shell now ships ASCII-safe bundle metadata, a non-null desktop CSP, local typography without remote Google font fetches, `nsis` plus `msi` bundle targets, and a reduced shell-open-only plugin surface; updated `apps/desktop-shell/src-tauri/src/backend.rs`, `apps/desktop-shell/src-tauri/src/lib.rs`, and `apps/desktop-shell/src-tauri/src/unity_runtime.rs` so packaged builds resolve bundled resources correctly, route backend mutable state into the Tauri app data or cache or log roots, and suppress stray Windows console windows during backend launch; and refreshed `docs/architecture/desktop-target.md`, `docs/architecture/desktop-runtime-flow.md`, `docs/architecture/local-storage.md`, `tasks/task-people.md`, and `tasks/task-queue.md` so current implementation and the remaining installer follow-up stay explicit. Evidence for this history entry is the successful terminal run of `py -3 scripts/prepare_desktop_bundle_resources.py`, the successful terminal run of `py -3 scripts/validate_desktop_bundle.py`, the successful terminal run of `py -3 scripts/validate_desktop_execution_surface.py`, the successful terminal runs of `npm run web:check`, `npm run web:build`, `npm run desktop:check`, and `npm run rebuild:check`, and the successful terminal run of `npm run desktop:build` producing `apps/desktop-shell/src-tauri/target/release/bundle/nsis/App Tro Ly_0.1.0_x64-setup.exe` plus `apps/desktop-shell/src-tauri/target/release/bundle/msi/App Tro Ly_0.1.0_x64_en-US.msi`. Manual validation required: target-machine installer smoke is still tracked separately under `P14`.
- D069: Completed `A18` by extending `ai-dev-system/control-plane/tools/mesh_ai_pipeline.py` plus `ai-dev-system/workflows/mesh_ai_refine.py` so the Mesh AI workflow can run Blender wrapper steps with a specified executable, write a repo-tracked execution summary, and then sync the sibling refinement or validation or handoff manifests back from the resulting root artifacts. The same pass refreshed `ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-intake.json`, `mesh-ai-azure-sakura-refinement-plan.json`, `mesh-ai-azure-sakura-validation-report.json`, `mesh-ai-azure-sakura-handoff.json`, and the new `mesh-ai-azure-sakura-execution-pass.json`, updated the Mesh AI validator and docs, and closed `P13` after the first target-machine Blender 5.1 run for the Azure Sakura sample. Evidence for this history entry is the successful terminal run of `python ai-dev-system/workflows/mesh_ai_refine.py --intake-manifest ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-intake.json --output-dir %TEMP%\mesh-ai-a18-live-pass --sync-lifecycle-manifests --execute-wrappers --blender-executable "C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" --execution-summary-path ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-execution-pass.json`, the resulting execution summary at `ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-execution-pass.json` showing five successful wrapper steps, the successful terminal run of `python -m unittest ai-dev-system.tests.test_mesh_ai_pipeline`, the successful terminal run of `python ai-dev-system/scripts/validate/validate_mesh_ai_pipeline.py`, and the successful terminal run of `python -m unittest ai-dev-system.tests.structure.test_mesh_ai_pipeline_structure`.
- D070: Completed `A19` by landing Unity-side Mesh AI handoff registry intake under `ai-dev-system/clients/unity-client/Assets/Scripts/Runtime/MeshAssetRegistry.cs` plus `Assets/Tests/EditMode/MeshAssetRegistryTests.cs`, wiring `AppCompositionRoot.cs` and `UnityBridgeClient.cs` so the active Unity runtime now loads export-ready handoff manifests for wardrobe foundations and room assets, supports lookup by asset id or asset name or target Unity path, and lets room-focus fallback use registry-backed room metadata without claiming live wardrobe equip completion. The same pass fixed the compile blocker in `Assets/Scripts/Runtime/AvatarRuntime.cs` so EditMode verification could run again. Evidence for this history entry is the successful terminal run of `C:\Program Files\Unity\Hub\Editor\6000.4.1f1\Editor\Unity.exe -batchmode -nographics -projectPath d:\antigaravity_code\app_tro_ly\ai-dev-system\clients\unity-client -runTests -testPlatform EditMode -testFilter LocalAssistant.Tests.EditMode.MeshAssetRegistryTests -testResults d:\antigaravity_code\app_tro_ly\tools\reports\unity-a19-editmode-results.xml -logFile d:\antigaravity_code\app_tro_ly\tools\reports\unity-a19-editmode.log`, the resulting `tools/reports/unity-a19-editmode-results.xml` showing `3 passed`, and the included `MeshAssetRegistryTests` coverage proving registry intake plus room-focus fallback plus `wardrobe.equipItem` rejection messaging for the deferred `A20` mapping step.
- D071: Completed `A20` by adding runtime wardrobe item lookup in `ai-dev-system/clients/unity-client/Assets/Scripts/Runtime/AvatarItemRegistry.cs`, wiring `ai-dev-system/clients/unity-client/Assets/Scripts/Runtime/UnityBridgeClient.cs` plus `ai-dev-system/clients/unity-client/Assets/Scripts/App/AppCompositionRoot.cs` so `wardrobe.equipItem` now resolves Mesh AI handoff aliases into live `AvatarRuntime.EquipItem(...)` calls, and checking in the placeholder-safe sample item `ai-dev-system/clients/unity-client/Assets/Resources/AvatarItems/AzureSakuraKimono.asset` so the current Unity runtime has a build-safe runtime mapping target. The same pass extended `ai-dev-system/clients/unity-client/Assets/Tests/EditMode/MeshAssetRegistryTests.cs` with both the successful equip case and the explicit missing-mapping rejection case, and refreshed the domain or tracker docs so current implementation notes no longer describe `A20` as planned-only work. Evidence for this history entry is the successful terminal run of `C:\Program Files\Unity\Hub\Editor\6000.4.1f1\Editor\Unity.exe -batchmode -nographics -projectPath d:\antigaravity_code\app_tro_ly\ai-dev-system\clients\unity-client -runTests -testPlatform EditMode -logFile d:\antigaravity_code\app_tro_ly\tools\reports\unity-a20-editmode-all.log`, the resulting `tools/reports/unity-a20-editmode-results.xml` showing `34 passed`, and the included `MeshAssetRegistryTests` coverage proving both the runtime-equip and missing-runtime-item branches. Current implementation: the bridge path is now live for registered runtime items, while production prefab hookup and broader asset population still remain placeholder-safe or manual follow-up under `P04`.
- D072: Completed `B01` by switching `ai-dev-system/clients/unity-client/Assets/Scripts/App/AssistantBootstrap.cs` so Unity now auto-boots `StandaloneRoomApp` instead of the legacy `AssistantApp`, adding `ai-dev-system/clients/unity-client/Assets/Scripts/App/StandaloneRoomCompositionRoot.cs` plus `StandaloneRoomApp.cs` so the default runtime now builds a placeholder-safe standalone room stage with a warm orthographic camera, directional light, four zone anchors, the existing room or bridge runtime stack, and a minimal `AvatarRootController` plus `AvatarEquipmentManager` plus `AvatarConversationBridge` root without loading `UI/MainUI`, and extending `ai-dev-system/clients/unity-client/Assets/Scripts/Runtime/RoomRuntime.cs` plus `ai-dev-system/clients/unity-client/Assets/Tests/EditMode/StandaloneRoomRuntimeTests.cs` so the new bootstrap path is covered by focused room-runtime assertions. Evidence for this history entry is the successful terminal run of `C:\Program Files\Unity\Hub\Editor\6000.4.1f1\Editor\Unity.exe -batchmode -nographics -projectPath d:\antigaravity_code\app_tro_ly\ai-dev-system\clients\unity-client -runTests -testPlatform EditMode -testFilter LocalAssistant.Tests.EditMode.StandaloneRoomRuntimeTests -testResults d:\antigaravity_code\app_tro_ly\tools\reports\unity-b01-editmode-results.xml -logFile d:\antigaravity_code\app_tro_ly\tools\reports\unity-b01-editmode.log`, the resulting `tools/reports/unity-b01-editmode-results.xml` showing `3 passed`, and the successful terminal run of `C:\Program Files\Unity\Hub\Editor\6000.4.1f1\Editor\Unity.exe -batchmode -nographics -projectPath d:\antigaravity_code\app_tro_ly\ai-dev-system\clients\unity-client -runTests -testPlatform EditMode -assemblyNames LocalAssistant.EditModeTests -testResults d:\antigaravity_code\app_tro_ly\tools\reports\unity-b01-editmode-all-results.xml -logFile d:\antigaravity_code\app_tro_ly\tools\reports\unity-b01-editmode-all.log`, with `tools/reports/unity-b01-editmode-all-results.xml` showing `37 passed`. Current implementation: Unity now boots into a placeholder-safe room runtime while production avatar visuals, room blockout art, and offline-task-viewer retirement remain follow-on work.
- D073: Mirrored the repo-side Unity configuration plus the landed `B00` or `B01` standalone-room runtime bundle into the new app-level project at `apps/unity-client/` by copying the Unity `Packages/` and `ProjectSettings/` surfaces plus the runtime or avatar or scene or settings or test assets needed for the standalone room bootstrap, then updating `apps/unity-client/Assets/Scripts/Runtime/MeshAssetRegistry.cs` so its default manifest lookup matches the new app-level project path. The same pass refreshed `apps/unity-client/README.md` to label the project as a mirror rather than the active source of truth and updated `tasks/task-queue.md` so current implementation notes now acknowledge the app-level Unity mirror while keeping `ai-dev-system/clients/unity-client/` as the active runtime path. Evidence for this history entry is the successful terminal run of `C:\Program Files\Unity\Hub\Editor\6000.4.1f1\Editor\Unity.exe -batchmode -nographics -projectPath d:\antigaravity_code\app_tro_ly\apps\unity-client -runTests -testPlatform EditMode -testFilter LocalAssistant.Tests.EditMode.StandaloneRoomRuntimeTests -testResults d:\antigaravity_code\app_tro_ly\tools\reports\apps-unity-b01-editmode-results.xml -logFile d:\antigaravity_code\app_tro_ly\tools\reports\apps-unity-b01-editmode.log`, the resulting `tools/reports/apps-unity-b01-editmode-results.xml` showing `3 passed`, and the imported package plus project surface now present under `apps/unity-client/Assets/`, `apps/unity-client/Packages/`, and `apps/unity-client/ProjectSettings/`.
- D074: Added the optional Blender MCP interactive lane under `ai-dev-system/control-plane/app/blender/` with a dedicated `blender-editor` profile, policy-gated capability matrix, preflight, assertions, upstream pin metadata, and a dedicated `blender_mcp_tool` strategy so the control plane can attach to Blender MCP without reusing the Unity-only runtime key. The same pass added repo-owned validation or run surfaces in `ai-dev-system/verify_blender_connection.py`, `ai-dev-system/scripts/run/run-blender-automation.ps1`, and `ai-dev-system/scripts/validate/validate-blender-mcp.ps1`, plus architecture and guidance updates in `docs/architecture/blender-mcp-integration.md`, `ai-dev-system/README.md`, and `docs/architecture/mesh-ai-blender-unity-integration.md`. Evidence for this history entry is the targeted unit-test pass for the new Blender runtime or capability or preflight or profile or strategy coverage together with the repo-side `list-profiles`, `list-capabilities --profile blender-editor`, and `verify_blender_connection.py` smoke outputs recorded on 2026-04-08. Manual validation required: upstream addon install and live Blender attach plus viewport screenshot remain tracked separately under `P15`.

## Notes

- The active assistant runtime lives in `ai-dev-system/clients/unity-client/` and `local-backend/`.
- `agent-platform/` is not present in the current repo snapshot; historical references treat it as an optional adjacent subsystem rather than a runtime dependency.
- Backend tests were re-verified on 2026-03-26 from `local-backend/` with `pytest -q`: `62 passed`.
- Unity Editor validation is still a separate manual-evidence step.
