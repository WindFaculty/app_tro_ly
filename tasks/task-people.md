# Task People - Local Desktop Assistant

Updated: 2026-04-09T03:15+07:00
Status values: TODO | DOING | DONE | BLOCKED
Ownership: `Pxx` = manual or off-repo work | `Axx` = AI-executable repo work tracked in `tasks/task-queue.md`

## How To Use This File

- Track work that cannot be completed safely from terminal-only repo access.
- Use this file for Unity Editor runs, target-machine checks, runtime installs, external assets, credentials, approvals, and sign-off.
- Keep active manual gates separate from historical done evidence.
- Keep unblock lists aligned with the active lane names in `tasks/task-queue.md`.

## Active Manual Gates

- `P02 | DOING | Run manual Unity runtime smoke tests for the live room, avatar, bridge, and packaged-runtime behavior. | Current state: repo-side Unity runtime now lives in `apps/unity-runtime/` and no longer contains the legacy business UI shell. Repo-side automated coverage currently proves room bootstrap plus mesh asset registry in EditMode and avatar bridge plus avatar state machine in PlayMode, but terminal-only work cannot prove clean Editor import, room boot visuals, bridge-driven focus changes, avatar presentation, or packaged-runtime startup behavior on the target machine. | Done when: Unity `6000.4.1f1` opens `apps/unity-runtime/` cleanly, the runtime boots without project-code console errors, room focus changes behave as expected, avatar presentation still works through the current placeholder-safe scene setup, and at least one packaged-runtime or standalone runtime launch has artifact evidence. | Unblocks: B-series runtime smoke, packaged runtime confidence, and future sync validation`
- `P04 | TODO | Provide the signed-off production avatar asset, animator expectations, and lip-sync expectations for the production avatar path. | Current state: the repo already contains placeholder avatar runtime controllers, prototype assets, validators, and probe scenes, but it does not yet have the final approved production asset handoff needed for completion claims. | Done when: the repo has the non-placeholder inputs required for final avatar integration and wardrobe-facing production expectations. | Unblocks: UI-4 Avatar + Customization production work`
- `P11 | TODO | Provide a Windows standalone Unity build artifact for the Tauri embed workstream and validate the first sidecar launch on a target machine. | Current state: repo-side Phase 4 scaffold can now resolve env-provided or repo-provided Unity executables, report status to React, and attempt sidecar launch, but the repo does not currently contain a Unity executable under `apps/unity-runtime/` or `release/`. | Done when: at least one Unity standalone build directory or executable is available to the desktop host through `APP_TRO_LY_UNITY_EXE`, `APP_TRO_LY_UNITY_BUILD_DIR`, or an agreed repo-side build location, and a first launch attempt has artifact evidence showing whether spawn works before native attach work continues. | Unblocks: RB-04 Phase 4 Unity embed validation`
- `P12 | TODO | Run the first typed React -> Tauri -> Unity bridge smoke on a target machine. | Current state: repo-side Phase 6 scaffold now defines packages/contracts/, starts a local websocket bridge in Tauri, and adds TauriBridgeRuntime plus typed envelope models in Unity, but there is still no machine-side evidence that the websocket handshake or typed command delivery works end to end. | Done when: a target-machine run shows bridge status moving to connected, at least one React route change delivers app.pageChanged, and at least one Unity event such as bridge.ready or avatar.stateChanged is observed back in the desktop shell with artifact evidence. | Unblocks: RB-06 Phase 6 runtime validation, RB-07 safe UI migration work`
- `P15 | TODO | Install and connect the upstream Blender MCP addon in the live Blender session, then capture the first target-machine attach or inspect smoke for the new `blender-editor` lane. | Current state: repo-side `A21` now provides the Blender MCP runtime, policy-gated capability matrix, validation helper, and `blender-editor` profile, but terminal-only repo work cannot install the upstream addon into the running Blender session or prove the live viewport screenshot path end to end. | Done when: the Blender addon is installed and connected on the target machine, telemetry is confirmed disabled, `python ai-dev-system/verify_blender_connection.py` reports the required safe tools, and at least one live `inspect --app blender-editor` run captures scene metadata plus a viewport screenshot artifact. | Unblocks: live Blender MCP attach validation and any future expansion beyond the v1 safe capability set`
- `P16 | TODO | Clear the live Unity CLI Loop startup blocker in the active Unity project and capture the first target-machine compile or test or log smoke against the shared Unity integration lane. | Current state: repo-side work now includes the manifest reference for `unity-cli-loop.git?path=/Packages/src`, hardened Windows CLI resolution, `python ai-dev-system/verify_unity_integration.py`, `python ai-dev-system/scripts/validate/validate_unity_cli_loop_e2e.py`, and `powershell -NoProfile -ExecutionPolicy Bypass -File ai-dev-system/scripts/run/run-unity-automation.ps1 -Lane integration cli-e2e`. Terminal verification also proved the local `uloop` CLI is installed and the Unity package resolves into `Library/PackageCache`, but the current Unity Editor session still does not register `Window/Unity CLI Loop/*` menus or start the server because Unity logs show duplicate assembly conflicts between `apps/unity-runtime/Assets/Plugins/Roslyn/*.dll` and `Packages/io.github.hatayama.uloopmcp/Plugins/CodeAnalysis/*`, followed by invalid script assemblies during `ReloadAssembly`. | Done when: the local Unity-side assembly conflict is cleared, the Unity CLI Loop server starts from the target `apps/unity-runtime/` Editor session, `python ai-dev-system/verify_unity_integration.py` reports a spawnable `uloop get-project-info` probe, and at least one live compile or test or log run records artifacts through the shared integration lane. | Unblocks: live Unity CLI Loop validation and any expansion of the optional execution lane beyond repo-side verification`

## Completed Manual Evidence

- `P01 | DONE | Historical pre-cutover Unity Editor pass before the `apps/unity-runtime/` move. | Current state: EditMode `21 passed` and PlayMode `33 passed` were validated on 2026-03-28 against the then-live Unity tree. | Role now: historical evidence record, not an active blocker.`
- `P13 | DONE | Run the first target-machine Mesh AI -> Blender -> validation -> Unity handoff smoke with actual asset inputs. | Current state: on 2026-04-07 the Azure Sakura sample wrapper plan was executed successfully on the target machine with Blender 5.1 at `C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`, using `python ai-dev-system/workflows/mesh_ai_refine.py --intake-manifest ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-intake.json --output-dir %TEMP%\mesh-ai-a18-live-pass --sync-lifecycle-manifests --execute-wrappers --blender-executable "C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" --execution-summary-path ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-execution-pass.json`. Evidence now lives in `ai-dev-system/asset-pipeline/manifests/mesh-ai-azure-sakura-execution-pass.json` plus the refreshed validation and handoff manifests and the produced root artifacts under `tools/reports/`, `tools/renders/`, and `bleder/`. | Role now: historical evidence and unblock record for A18 and A19.`
- `P03 | DONE | Install and configure speech runtimes on the target machine as needed for end-to-end validation. | Current state: STT (faster-whisper) and TTS (ChatTTS) were configured, their models were downloaded from HuggingFace, a PyTorch 2.6 security block during ChatTTS loading was patched, and both services were verified end-to-end through the local API. | Role now: historical evidence and unblock record, not an active blocker.`
- `P05 | DONE | Validate the packaged release folder on a clean Windows machine and record follow-up issues. | Historical note: this is no longer an active blocker. Evidence recorded earlier included a successful release-folder run and follow-up runtime observations.`

## Future Manual Prerequisites

- `P06 | BLOCKED | When live Google integration validation starts, provide OAuth credentials, consent-screen redirect setup, a test inbox, a test calendar, and project setup details. | Current state: repo-side Gmail auth, inbox, draft, send, and email-to-task surfaces now exist from A09, and repo-side Google Calendar auth plus agenda plus event CRUD surfaces now exist from A10, but the repo still has no real Google OAuth client credentials or shared test mailbox or calendar to prove live provider behavior on a target machine. | Done when: the backend has a working Google OAuth client id or secret plus allowed redirect URIs, a test inbox and calendar are available, and a target-machine run captures live connect or read or send evidence for Gmail plus live connect or read or update evidence for Google Calendar. | Unblocks: live Google provider smoke for A09 A10`
- `P07 | BLOCKED | When automation work starts, provide accounts, environments, and Windows permissions for safe validation. | Unblocks: A11 future automation work`
- `P08 | BLOCKED | When sync work starts, define device topology, network assumptions, and storage location. | Unblocks: A22 A23`
- `P09 | TODO | Manually validate the Windows GUI agent on the intended target desktop session, including the emergency-stop hotkey, focus behavior, self-healing behavior, and any future profile selectors beyond Notepad and Calculator. | Done when: at least one selector-failure scenario safely stops on ambiguous healing, at least one deterministic healing hint succeeds with artifact evidence, any configured Vision LLM fallback has been exercised or explicitly left disabled on the target machine, and hotkey plus focus behavior still match expectations during real desktop runs. | Unblocks: A26 future GUI-agent expansion work`
- `P10 | TODO | Manually validate the hybrid `unity-editor` control plane on the target Unity Editor desktop session. | Done when: the live capability matrix has been reviewed on the target machine, representative MCP-backed and GUI-fallback capabilities have been exercised with artifact paths recorded for failures, and planned graph capabilities that still show `unsupported` are recorded as backend follow-up instead of signed off as done. | Unblocks: A26`
- `P14 | TODO | Run the first target-machine installer smoke for the packaged Tauri desktop app. | Current state: repo-side `A15` verification now produces NSIS and MSI installers under `apps/desktop-shell/src-tauri/target/release/bundle/`, stages bundled backend plus customization resources, and routes backend writes into the Tauri app data or cache or log roots, but there is still no target-machine evidence that the installer launches cleanly and reaches backend ready or partial health outside the repo workspace. | Done when: a Windows target-machine run installs either packaged artifact, launches the desktop app successfully, confirms backend ready or partial health from the bundled runtime, and records the app data or cache or log paths used by the packaged shell. | Unblocks: future desktop release review`

## Rule

- If a task needs Unity visuals, a target machine, external binaries, external credentials, external asset handoff, or external approval, keep it here instead of forcing it into the AI queue.
- Only `P02`, `P04`, `P11`, `P12`, `P14`, `P15`, `P16`, and the future prerequisites `P06` through `P10` should be described as active or potential blockers now.
- Treat `P01`, `P03`, `P05`, and `P13` as completed evidence records rather than current gates.
