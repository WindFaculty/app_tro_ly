# Migrate

Current implementation: this folder is reserved for later repo-side migration helpers.

Phase 7 does not move live runtime roots again. The current need is to standardize run, validate, and package entry points while keeping root operational scripts stable.
