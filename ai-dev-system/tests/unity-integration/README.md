# Unity Integration Tests

Current implementation:

- Unity EditMode and PlayMode coverage still lives in `../../clients/unity-client/Assets/Tests/`.
- Terminal-only validation in Phase 7 does not replace Unity Editor smoke or runtime evidence.

This bucket exists so `ai-dev-system/tests/` now reflects the intended ownership map for subsystem testing.
