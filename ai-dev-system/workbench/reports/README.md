# Workbench Reports

Current implementation: review artifacts still live under root `tools/reports/` and `tools/renders/`.

Current source-of-truth material:

- `../../../tools/reports/`
- `../../../tools/renders/`

Planned work:

- move report and render artifacts under `ai-dev-system/workbench/reports/` once the asset-pipeline and release flow stop assuming the root paths
